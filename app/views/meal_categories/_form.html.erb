<%= form_with(model: meal_category, local: true) do |form| %>
  <% if meal_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(meal_category.errors.count, "error") %> prohibited this meal_category from being saved:</h2>

      <ul>
      <% meal_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="container">
    <div class="field">
      <%= form.label :Nome, class:"field" %>
      <%= form.text_field :name, class:"form-control  backoffice_input" %>
    </div>
    <br>

      <div class="actions">
        <%= form.submit "Atualizar", class:"button_form" %>
        <%= link_to 'Voltar', meal_categories_path, class:"btn btn-secondary" %>
      </div>
    </div>

  </div>
<% end %>
